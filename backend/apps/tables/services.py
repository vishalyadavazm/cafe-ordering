"""QR generation (step 5): build the public URL for a table's qr_token and render
a PNG with the `qrcode` lib; 'download all' zips every table's QR."""
# def build_qr_url(qr_token): return f"https://app.example.com/t/{qr_token}"
# def render_qr_png(url) -> bytes: ...
# TODO
